{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd GrindHouse && npm ci && npm run build && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd GrindHouse && php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
