{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd GrindHouse && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd GrindHouse && php -S 0.0.0.0:$PORT -t public",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
